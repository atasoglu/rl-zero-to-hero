[project]
name = "004-td-lambda"
version = "0.1.0"
requires-python = ">=3.10"
description = "TD(λ) with eligibility traces on discretized CartPole"
dependencies = [
    "rl-common",
    "gymnasium[classic-control]==1.1.1",
    "numpy>=2.0",
    "matplotlib>=3.9",
]

[tool.uv.sources]
rl-common = { path = "../common", editable = true }

[dependency-groups]
dev = ["ipykernel"]
