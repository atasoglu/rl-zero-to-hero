[project]
name = "001-q-learning"
version = "0.1.0"
requires-python = ">=3.10"
description = "Q-Learning on FrozenLake and Taxi environments"
dependencies = [
    "rl-common",
    "gymnasium[toy-text]==1.1.1",
    "numpy>=2.0",
    "matplotlib>=3.9",
]

[tool.uv.sources]
rl-common = { path = "../common", editable = true }

[dependency-groups]
dev = ["ipykernel"]
