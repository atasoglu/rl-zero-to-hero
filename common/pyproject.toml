[project]
name = "rl-common"
version = "0.1.0"
requires-python = ">=3.10"
description = "Shared utilities for the RL learning modules"
dependencies = [
    "gymnasium==1.1.1",
    "numpy>=2.0",
    "matplotlib>=3.9",
    "tqdm>=4.66",
]
